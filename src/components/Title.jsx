

const Title = ({title}) => {
 
  return (
    <div>
      <h2  className=" font-bold w-full block  text-5xl pb-3 mb-10 border-b-4 border-cyan-500 text-pink-700">{title }</h2>
    </div>
  );
};

export default Title;